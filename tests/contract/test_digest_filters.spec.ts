/**
 * TODO: `/v1/digests?tags=&search=` の実装後にOpenAPI契約へ差し替える。
 */

describe('GET /v1/digests (filters) RED', () => {
  it('タグ/検索フィルタ契約テストは未実装', () => {
    throw new Error('contract test for filters not implemented yet');
  });
});
