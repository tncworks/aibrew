/**
 * TODO: 実装後にOpenAPI契約に基づいたSchemathesis/Pactumテストへ置き換える。
 * 現時点ではTDDのRED段階として失敗するテストを配置する。
 */

describe('GET /v1/digests 契約テスト (RED)', () => {
  it('まだ実装されていないため意図的に失敗させる', () => {
    throw new Error('契約テストが未実装のため失敗します');
  });
});
