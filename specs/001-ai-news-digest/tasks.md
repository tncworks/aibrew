---

description: "Task list for 生成AIニュースダイジェスト (07:00 JST版)"

---

# タスク: 生成AIニュースダイジェスト

**入力**: `/specs/001-ai-news-digest/` 直下の設計ドキュメント  
**前提**: plan.md（必須）, spec.md（ユーザーストーリー参照）, research.md, data-model.md, contracts/

**テスト**: 仕様でTDDと契約テストが要求されているため、各ユーザーストーリーでテストタスクを明示。  
**構成**: タスクはユーザーストーリー単位でグルーピングし、独立実装と独立テストを保証する。

## 形式: `[ID] [P?] [Story] 説明`

- **[P]**: 並列実行可能（ファイル競合なし、依存なし）  
- **[Story]**: 該当ユーザーストーリー (例: US1, US2)  
- 説明には必ず正確なファイルパスを含めること

---

## フェーズ1: セットアップ（共通インフラ）

**目的**: モノレポ構成やMakeコマンドなどの土台を整備し、CI/CDとエージェントワークフローに備える。

- [X] T001 `src/`, `tests/`, `config/environments/`, `infra/terraform/`, `specs/001-ai-news-digest/contracts/` など計画に沿ったディレクトリを作成  
- [X] T002 `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml` を生成し Next.js 14 + Cloud Run Jobs 依存を追加  
- [X] T003 `Makefile` に `deps/build/test/lint/dev-api/dev-web/job-*` コマンドを定義し Quickstart 記載の手順と一致させる  
- [X] T004 `.env.example` および `config/environments/{dev,stg,prod}.json` の雛形を作成し Firestore/Vertex/Cloud Run 変数を整理  
- [X] T005 `.github/workflows/ci.yml` を追加し `check-prerequisites`, `make deps`, `make lint`, `make test`, `make e2e` を実行  

---

## フェーズ2: 基盤整備（ブロッカー）

**目的**: すべてのユーザーストーリーが依存する共通ライブラリとIaCを構築。ここが完了するまでUS作業は禁止。

- [X] T006 `src/services/config/index.ts` に環境JSON + `.env` を読み込む型付き設定ローダーを実装  
- [X] T007 [P] `src/services/firestore/admin.ts` でFirestore Admin初期化とエミュレータ切替を整備  
- [X] T008 [P] `src/services/observability/{logging,metrics}.ts` を作り構造化ログとメトリクス送信ヘルパーを実装  
- [X] T009 [P] `src/services/llm/vertex_client.ts` にVertex AI Text Bison/Geminiクライアントとレートリミット、指数バックオフを実装  
- [X] T010 `src/cli/cron/digest_pipeline.ts` にCloud Run Jobs実行スケルトン（fetch→summarize→publish連携、スロット引数受取）を定義  
- [X] T011 `infra/terraform/main.tf` と `infra/terraform/variables.tf` を用意し Cloud Run, Cloud Scheduler (05:30/06:00/06:30), Firestore, Secret Manager をIaC化  
- [X] T012 `tests/setup/jest.setup.ts` と `tests/setup/firestore-emulator.ts` を用意し Jest/Firestoreエミュレータの共通初期化を整備

**チェックポイント**: 設定/Firestore/LLM/IaC/ジョブ骨格/テスト基盤がすべて揃ったことを確認。

---

## フェーズ3: ユーザーストーリー1 - 5分で読める日次ダイジェスト（優先度: P1） 🎯 MVP

**ゴール**: 1日最大10件の生成AIダイジェストを07:00 JSTまでに公開し、要約・関連ソース・元記事リンクを含むページを提供する。  
**独立テスト**: 指定日ダイジェストを開くと10件以下でタイトル/要約/リンク/出典ロゴ/更新時刻/07:00バナー状態がE2Eで確認できる。

### ユーザーストーリー1のテスト

- [X] T013 [P] [US1] `tests/contract/test_digest_listing.spec.ts` で `/v1/digests` 契約テストを追加（entries/readMore/status検証）  
- [X] T014 [P] [US1] `tests/integration/test_digest_generation.spec.ts` を作成し Firestoreエミュレータでfetch→summarize→publishパイプラインを検証  
- [X] T015 [P] [US1] `tests/e2e/test_digest_page.spec.ts` (Playwright) で1日10件＋07:00 SLOバナーを検証

### ユーザーストーリー1の実装

- [X] T016 [P] [US1] `src/models/source_feed.ts` `src/models/article_candidate.ts` にSourceFeed/ArticleCandidateスキーマとバリデーションを実装  
- [X] T017 [P] [US1] `src/models/digest_entry.ts` と `src/models/digest_run.ts` にDigestEntry/DigestRunモデル・インデックス定義を追加  
- [X] T018 [P] [US1] `src/services/digest_pipeline/aggregator.ts` を実装し重複排除・順位付け・read-more振り分けロジックを追加  
- [X] T019 [US1] `src/cli/cron/jobs/fetch_sources.ts` にQiita/Zenn/IT系RSS取得＋SourceFeed更新＋ArticleCandidate作成を実装  
- [X] T020 [US1] `src/cli/cron/jobs/summarize_articles.ts` でVertex AIを呼び出し summary/tags/read_time/confidence を保存する処理を追加  
- [X] T021 [US1] `src/cli/cron/jobs/publish_digest.ts` でapproved候補をDigestEntryへ昇格し `DigestRun` を更新・フォールバック判定する処理を実装  
- [X] T022 [US1] `src/cli/cron/digest_pipeline.ts` に05:30/06:00/06:30スロット実行と再試行、Slack通知トリガーを実装  
- [X] T023 [US1] `src/web/app/api/v1/digests/route.ts` でFirestoreからentries/readMore/statusを返却しキャッシュヘッダとSLOバナー判定を実装  
- [X] T024 [US1] `src/web/app/(public)/digest/[date]/page.tsx` を作成しSSRで10件以内のエントリ＋関連ソース折り畳み＋空デー時の導線を描画  
- [X] T025 [US1] `src/services/observability/logging.ts` と各ジョブ/APIに構造化ログ・Cloud Monitoringメトリクス送信を組み込み

**チェックポイント**: US1の契約/統合/E2EテストがGREENで、07:00 JST公開SLOとフォールバック表示が実装されている。

---

## フェーズ4: ユーザーストーリー2 - 興味タグでの絞り込み（優先度: P2）

**ゴール**: ユーザーがタグや検索語を即時適用し、5件以下にまで一覧を絞り込める。  
**独立テスト**: UIで複数タグ選択＋検索を行うと対応するエントリのみが表示され、適用中フィルタが表示されることをPlaywrightで確認。

### ユーザーストーリー2のテスト

- [X] T026 [P] [US2] `tests/contract/test_digest_filters.spec.ts` を追加し `/v1/digests?tags=&search=` の応答スキーマを検証  
- [X] T027 [P] [US2] `tests/e2e/test_tag_filtering.spec.ts` でタグ/検索UIの動的フィルタと件数を検証

### ユーザーストーリー2の実装

- [X] T028 [P] [US2] `src/models/tag_facet.ts` と `src/scripts/seed_tag_facets.ts` にTagFacet定義と初期データ投入スクリプトを実装  
- [X] T029 [P] [US2] `src/services/digest_pipeline/filter_query.ts` にタグ配列/全文検索を扱うFirestoreクエリヘルパーを実装  
- [X] T030 [US2] `src/web/app/api/v1/digests/route.ts` を拡張し `tags`/`search` クエリと `readMore` の分岐ロジックを追加  
- [X] T031 [US2] `src/web/components/filters/TagFilterPanel.tsx` にマルチセレクトUIとBadge表示、選択状態のURL同期を実装  
- [X] T032 [US2] `src/web/app/(public)/digest/[date]/page.tsx` にTagFilterPanelと検索ボックス、ローディング/Empty状態を統合  
- [X] T033 [US2] `src/services/observability/metrics.ts` にタグ利用メトリクス送信とCloud Monitoringダッシュボード連携を追加

**チェックポイント**: `/v1/digests` がタグ/検索に対応し、UIテストがGREENで5分読了体験を維持できる。

---

## フェーズ5: ユーザーストーリー3 - 自動要約品質ゲート（優先度: P3）

**ゴール**: 要約品質を自動検証し、しきい値未満は再要約→再検証→監査ログ記録を完全自動で行う。  
**独立テスト**: 品質ゲートが低スコア要約を差戻して再要約→合格→公開まで自動で進むE2Eテストが成功。

### ユーザーストーリー3のテスト

- [ ] T034 [P] [US3] `tests/contract/test_review_queue.spec.ts` を追加し `/v1/review/queue` および approve/reject エンドポイント契約を検証  
- [ ] T035 [P] [US3] `tests/integration/test_quality_gate.spec.ts` で品質スコアしきい値・再要約キュー・監査ログを検証

### ユーザーストーリー3の実装

- [ ] T036 [P] [US3] `src/services/quality_gate/rules.ts` に事実チェック/数値整合性/トーン検証ロジックを実装  
- [ ] T037 [US3] `src/services/quality_gate/retry_queue.ts` で再要約キュー管理と最大3回までの再試行/バックオフを実装  
- [ ] T038 [US3] `src/services/review_queue.ts` にEditorialReview/監査ログの読み書きと自動承認トリガーを実装  
- [ ] T039 [US3] `src/web/app/api/v1/review/queue/route.ts` にレビュー待ちAPI (Firebase Auth JWT検証) を実装  
- [ ] T040 [US3] `src/web/app/api/v1/review/entries/[id]/approve/route.ts` と `reject/route.ts` に自動承認/差戻しエンドポイントを実装  
- [ ] T041 [US3] `src/cli/cron/jobs/summarize_articles.ts` を拡張し品質ゲートの結果で自動再要約・監査ログ更新を行う  
- [ ] T042 [US3] `src/web/app/(admin)/review/page.tsx` にレビュー状況UIと自動承認ログビューアを実装

**チェックポイント**: 自動品質ゲートが再要約と公開判定を自律処理し、契約/統合テストがGREEN。

---

## フェーズ6: 仕上げ／横断的対応

**目的**: ドキュメント整備、監視、コスト/SLO可視化など複数ストーリーで共有する作業。

- [ ] T043 `specs/001-ai-news-digest/quickstart.md` と `README.md` に07:00 SLO/Cloud Schedulerスロット/新APIのランブックを追記  
- [ ] T044 `specs/001-ai-news-digest/contracts/digest-api.yaml` を最終実装と照合しスキーマ差分を更新  
- [ ] T045 `infra/terraform/monitoring.tf` を追加し Cloud Monitoring アラート（06:45 SLO/コスト$40/月）を構築  
- [ ] T046 [P] `tests/performance/test_digest_performance.spec.ts` を追加し `/v1/digests` p95 < 800ms (50並列) を計測  
- [ ] T047 `AGENTS.md` と `.specify/scripts/bash/update-agent-context.sh` 実行結果をコミットし、エージェントガイドを最新化  
- [ ] T048 `make test`/`make lint`/`make build` 結果を記録し、未解決事項を `NEEDS CLARIFICATION` ラベル付きでplan.mdに追記

---

## 依存関係と実行順

1. **セットアップ (Phase 1)** → リポジトリ整備とCI基盤を準備。  
2. **基盤 (Phase 2)** → 設定/Firestore/LLM/Observability/IaC/テスト基盤が完成したらユーザーストーリー着手可。  
3. **US1 (Phase 3, P1)** → MVPかつ07:00 JST公開SLOに直結。US2/US3はUS1がリリース可能になってから着手。  
4. **US2 (Phase 4, P2)** → US1のAPI/データを元にタグ/検索機能を追加。  
5. **US3 (Phase 5, P3)** → 上位フェーズで生成されたArticleCandidate/DigestEntryを利用して自動品質ゲートを実装。  
6. **Polish (Phase 6)** → すべてのユーザーストーリー後に横断的な監視・ドキュメント・性能改善を実施。

## 並列実行ヒント

- `[P]` が付いたタスクはファイル競合がない限り並列実行可（例: Firestore/LLM/Observabilityヘルパー、テスト追加、モデル実装）。  
- フェーズ2完了後は US2/US3 をUS1と並列に着手できるが、同一ファイル（例: `src/web/app/api/v1/digests/route.ts`）への同時変更は避ける。  
- E2E/契約テスト (`tests/contract/*`, `tests/e2e/*`) は実装前に独立して準備できるため、自動化担当者が並列で進めやすい。

## 実装戦略

1. **MVP優先**: フェーズ1→2→3を終え、US1を07:00 JST配信SLO込みでリリース。  
2. **インクリメンタルデリバリー**: US1を本番適用後、US2タグフィルタ → US3品質ゲートの順で段階的に機能を追加。  
3. **継続的テスト**: 各フェーズで `make test` を実行しRED→GREENを証跡として残す。  
4. **デプロイ計画**: dev→stg→prodを `make deploy ENV=<env>` + GitHub Actions で昇格させ、Cloud Schedulerスロットと監視アラートを検証。
